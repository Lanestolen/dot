- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash_aliases: dotfiles/bash_aliases
    # for latexmk -> remove more files
    ~/.latexmkrc: dotfiles/latexmkrc
